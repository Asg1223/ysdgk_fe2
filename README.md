# カラーキャッチ！ — Raspberry Pi 3 文化祭ゲーム

赤いカードを持って手を振ると、大画面の自分の映像の上に出てくる ◯ を「触って」得点する
30秒の体感ゲーム。1人あたり約40秒で回るので行列がさばけて、待っている人も画面を見て楽しめる。

- 入力：USB / Pi カメラ（色マーカー追跡）
- 出力：HDMI モニタ、スピーカー（効果音は起動時に合成するので音源ファイル不要）
- 遊び方は2モード：**つかむモード**（手をグーにして取る・既定）と タッチモード（触れるだけ）
- 手の見つけ方も2方式：**素手（肌色＋動き）** と 色マーカー（軍手・カード）。`M` キーでその場で切替
- スタート方法は3通り：物理ボタン / スペースキー / **かざす・つかむだけ（部品ゼロ・自動切替）**
- 当日のランキングを自動保存・待機画面に表示

## 1. 用意するもの

| もの | 備考 |
|---|---|
| Raspberry Pi 3 + microSD(8GB以上) | Raspberry Pi OS デスクトップ版 |
| USB カメラ または Pi カメラ | 640x480 で十分。広角だと遊びやすい |
| HDMI モニタ / TV | 大きいほど盛り上がる |
| スピーカー | 3.5mm でもUSBでも可 |
| （任意）タクトスイッチ + LED + 330Ω抵抗 | **無くても完全に運用できる** → 第4章 |
| （任意）赤い軍手 | **素手モードなら不要。** 照明や背景が厳しいときの保険として持っておくと安心 |
| 赤い厚紙（タッチモード）| `marker_card.pdf` を印刷して厚紙に貼る |

### 配線（GPIO・BCM番号）※部品がある場合だけ

```
ボタン :  GPIO17 ---- スイッチ ---- GND      （内部プルアップを使うので抵抗不要）
LED    :  GPIO27 ---- 330Ω ---- LED(+) ... LED(-) ---- GND
```

ピン番号を変えたいときは `config.json` の `gpio.button_pin` / `gpio.led_pin`。

## 2. インストール

```bash
unzip colorcatch.zip && cd colorcatch
chmod +x *.sh           # ← 「権限がありません」対策。最初に1回だけ
./setup.sh              # USBカメラのとき
./setup.sh --picamera   # Piカメラ(リボンケーブル)のとき
```

**「Permission denied / 権限がありません」と出たら**
zip を Windows・Mac や USB メモリ経由で展開すると実行権限が外れます。上の `chmod +x *.sh` で直ります。
それでもダメなとき（FAT/exFAT の USB メモリ上など、権限を保存できない場所）は、頭に `bash` を付ければそのまま動きます:

```bash
bash setup.sh
bash run.sh
python3 main.py         # run.sh を使わず直接起動してもよい
```

`tar.gz` 版（`colorcatch.tar.gz`）を Pi 上で `tar xzf colorcatch.tar.gz` すれば権限は保たれます。

`setup.sh` は apt から OpenCV / pygame を入れる（pip でビルドすると Pi 3 では数十分かかる）。
最後に自己テストが走り、`結果: OK` が出れば準備完了。

## 3. 起動と操作

```bash
./run.sh                  # 本番（フルスクリーン）
./run.sh --windowed       # ウィンドウ表示
./run.sh --no-camera      # カメラなし、マウスで動作確認
./run.sh --debug          # FPS とマスク画像を表示
./run.sh --calibrate      # 色あわせ画面から起動
python3 selftest.py       # 画面なしの動作テスト
```

| キー | 動作 |
|---|---|
| スペース / Enter / 物理ボタン | スタート・次へ |
| C | 色あわせ（キャリブレーション）画面 |
| M | 素手 ⇄ 色マーカー の切替 |
| D | デバッグ表示の切替 |
| `[` `]` | 色の許容幅を狭める / 広げる |
| `-` `=` | 明るさしきい値を下げる / 上げる |
| ESC / Q | 終了（色あわせ画面では戻る） |

## 4. 素手で遊ぶ（手袋なし）

既定は素手モード（`config.json` の `marker.mode: "skin"`）。何も身につけずに手をかざすだけで動きます。

### なぜ肌色だけで探さないか

教室の机は木目、段ボール、掲示物、そして**自分の顔**まで肌色に近く、
肌色だけで探すと机を掴んでしまいます。そこで3段構えで絞っています。

1. **肌色**（YCrCb の Cr/Cb）… 明るさの影響を受けにくい色空間で肌を拾う
2. **覚えた背景との差**（背景差分）… 机・壁・掲示物は「背景」なので消える。
   瞬間の動きではなく**背景画像との差**を見ているので、**手を止めても消えません**
3. **顔をよける**… 15フレームに1回だけ顔を検出し（160x120に縮小して実行、約3ms）、
   顔と首の範囲をマスクから外す

このため**設営したら必ず「背景を覚える」**手順が要ります（下記の①）。
机の配置を変えたり、置いてある物を動かしたら覚え直してください。

### 肌の色の違いについて

肌の明るさは主に Y（明るさ）に出て、Cr/Cb はあまり動きません。既定値は
一般的な肌色を広くカバーする範囲で、色あわせをしても**一般範囲からはみ出さないよう内側に収める**
実装にしてあります。明るい肌・中間・濃い肌の3通りで検出率を自己テストしています
（`python3 selftest.py` の [7][8]）。

### 素手モードの色あわせ（4ステップ）

`C` キー →  ボタン（スペース）で1つずつ進みます。

1. **背景を覚える** … 手を画面の外に出して押す
2. **肌の色** … 枠いっぱいに手を入れて押す
3. **パー** … 指を大きく開いて押す
4. **グー** … 握って押す

`M` キーでいつでも 素手 ⇄ 色マーカー を切り替えられます（手順も自動で切り替わります）。
うまくいかない日は迷わず赤い軍手（色マーカー方式）に逃げてください。**そのための M キーです。**

| 症状 | 対処 |
|---|---|
| 机や段ボールを掴んでしまう | ①の「背景を覚える」をやり直す。机に濃い色の布をかけると劇的に安定する |
| 顔にカーソルが行く | 顔検出が効いていない可能性。`skin.face_filter` が true か確認。立ち位置をカメラから1.5m以上に |
| 半袖で腕まで拾う | 腕まで含めると握り判定が鈍る。長袖か、カメラを少し上向きにして手だけ写す |
| 全然反応しない | 背景の学習時に手が写っていた可能性 → ①からやり直し。`M` で軍手モードに切り替えるのが確実 |

> プレイ中は背景の更新を自動で止めています（手を止めたまま数秒たっても消えないように）。
> 待機画面に戻ると再開するので、時間帯で照明が変わっても追従します。

## 5. つかむモード（手を握って取る）

`config.json` の `game.mode` が `"grab"` のとき、**◯に手を重ねてグーに握る**と得点します。
素手でも軍手でも同じように動きます。
触れただけでは取れないので、当てずっぽうに手を振り回すだけでは点が入らず、
見ている人にも「掴んだ」瞬間が分かって盛り上がります。✖ も掴まなければ減点されません。

- 手袋の輪郭の **fill 値（輪郭面積 ÷ 最小外接円の面積）** で開閉を判定しています。
  パーは指の間が空くので 0.4 前後、グーはほぼ円なので 0.9 前後。**面積の比なので、
  カメラからの距離が変わっても値がほとんど動かない**のがこの方式の狙いです
- MediaPipe などの手の骨格推定は Pi 3 では重すぎるため使っていません。この方式なら追加負荷は 1ms 未満
- しきい値は `C` の色あわせで **パー→グーを実演するだけ**で自動決定されます（下記）
- 触るだけの遊び方に戻すなら `game.mode` を `"touch"` に

### 開閉の登録

色あわせ画面（`C` キー）の後半2ステップで **パー → グー** を実演すると、
しきい値がその中間に自動設定され `config.json` に保存されます。
画面に `fill` の実測値と「グー／パー」の判定が出るので、手を開閉して
正しく切り替わるか確認してください。

| 症状 | 対処 |
|---|---|
| 握っていないのに取れる | パーのとき指をしっかり開く。手前に腕が写り込んでいないか（袖をまくる） |
| 握っても取れない | ③のグー登録をやり直す。`marker.grab_on` を少し下げてもよい |
| 判定がばたつく | 照明が暗い。`-` でしきい値を下げるか、手元にライトを当てる |

## 6. GPIO部品が無い場合

ボタンもLEDも無くて構いません。**GPIOが使えないと自動で「かざしてスタート」に切り替わります。**

### A. かざしてスタート（推奨・部品ゼロ）

待機画面の左に ◎ の的が出ます。来場者がマーカーをそこに **1.8秒重ねる**と、
リングが金色に溜まりきってゲームが始まります（つかむモードでは
**的の上で手を握るだけ**、0.7秒でスタート）。結果画面も同じ的で「もう一度」。
**係の人が何も操作しなくても勝手に回り続ける**ので、むしろボタンより運営が楽です。

- 待ち時間は `config.json` の `input.hold_seconds`（短いと誤爆、長いと待たされる。1.5〜2.5 が目安）
- ボタンを付けた状態でも両方使いたいなら `input.always_show` を `true` に
- 切りたいときは `input.hold_to_start` を `false`（キーボード運用になります）

### B. キーボードのスペースキー

USBキーボードを台に置き、係の人がスペースを押す。お菓子の箱に穴を開けて
キーボードを隠し、スペースキーの上にペットボトルのキャップを貼ると、それらしい大きなボタンになります。

### C. 100円ショップのBluetoothシャッターリモコン

スマホ用の自撮りリモコンはキーボードとして認識されるものが多く、ペアリングするだけで
スペース/Enter が飛びます（機種により Volume キーのこともあるので事前確認を）。
ワイヤレスプレゼンターや USB マウスでも代用できます（クリックでスタート）。

### LEDの代わり

LEDが無くても、待機中は「かざしてスタート」の点滅、プレイ中は残り時間バーが画面に出るので
状態は見て分かります。**電子部品はひとつも要りません。**

## 7. 色あわせ（当日いちばん大事）

会場の照明は教室とちがうので、**設営したら必ず 1 回やる**。

1. `C` キーで色あわせ画面
2. 画面中央の黄色い枠にマーカーを入れる（画面いっぱいでなく、枠を埋める程度）
3. ボタン（またはスペース）を押す → 「色を登録しました」と出れば成功
4. 右下のマスク画像で、マーカーだけが白くなっているか確認
   - 白い部分が広すぎる → `[` で許容幅を狭める
   - マーカーが写らない → `-` でしきい値を下げる、または照明を当てる
5. 設定は `config.json` に自動保存されるので、次の起動から有効

窓際で日が傾くと色が変わる。**昼と午後で 1 回ずつやり直す**と安定する。

## 8. 当日の運営

- 想定回転率：1人 40〜50 秒（説明10秒＋カウント4秒＋本編30秒＋結果）→ **1時間で約70〜80人**
- 立ち位置にビニールテープで印をつける（カメラから1.5〜2m）。近すぎると手が画面外に出る
- 係は2人：①受付・マーカー渡し ②スタートボタンと呼び込み
- マーカーは2枚以上用意（渡す用と戻ってくる用で回す）
- ランキングは `scores.json` に保存される。日付ごとに集計されるので当日分だけが表示される
- 表彰したいときは終了後に `python3 -c "import json;d=json.load(open('scores.json'));print(sorted(d,key=lambda r:-r['score'])[:10])"`

## 9. 調整（config.json）

| 項目 | 意味 | 迷ったら |
|---|---|---|
| `marker.mode` | `skin`=素手 / `color`=色マーカー | 迷ったら skin、不安定なら color |
| `skin.diff_threshold` | 背景との差の判定（大きいほど厳しい）| 28 前後 |
| `skin.face_filter` | 顔を除外するか | true 推奨 |
| `game.mode` | `grab`=握って取る / `touch`=触れば取れる | 小さい子が多い日は touch |
| `game.duration` | 制限時間(秒) | 行列が長い日は 20 に |
| `game.spawn_interval_start/end` | 出現間隔(秒)。end に向かって速くなる | 小さいほど難しい |
| `game.bomb_rate` / `bonus_rate` | ✖ と ★ の出現率 | 小さい子が多いなら bomb を 0.10 に |
| `marker.grab_on` / `grab_off` | 握り判定のしきい値（色あわせで自動設定）| 手動で触らなくてよい |
| `input.hold_seconds` | かざしてスタートの秒数 | 1.5〜2.5 |
| `marker.cursor_radius` | 当たり判定の広さ(px) | 当たらないと言われたら 60 へ |
| `marker.min_area` | この面積未満は無視 | 誤検出が多いなら大きく |
| `screen.width/height` | 解像度 | Pi 3 では 1280x720 まで |
| `camera.width/height` | 処理解像度 | 上げると重くなる。320x240 推奨 |

## 10. 困ったとき

| 症状 | 対処 |
|---|---|
| カーソルが出ない | `D` でマスク確認 → 色あわせやり直し。蛍光灯の反射で白飛びしていないか |
| カーソルが他の物に飛ぶ | 背景に同じ色の物がある。机に布をかける／`[` で許容幅を狭める |
| 左右が逆 | `config.json` の `camera.mirror` を切り替える |
| 音が出ない | `amixer cset numid=3 1`（3.5mm出力に固定）／`config.json` の `audio.enabled` |
| 画面が暗くなる | `./install_autostart.sh` を実行（スクリーンセーバー停止も入る） |
| `./run.sh: Permission denied` | `chmod +x *.sh`、または `bash run.sh` / `python3 main.py` |
| `bad interpreter: ^M` | Windowsの改行が混ざっている → `sed -i 's/\r$//' *.sh` |
| 動きがカクつく | `screen` を 1280x720 → 800x480 に、`camera` は 320x240 のままで |
| ボタンが効かない | 配線を確認。キーボードのスペース、または第4章の「かざしてスタート」で運営できる |
| 勝手にゲームが始まる | `input.hold_seconds` を伸ばす。◎の位置に人が立たない導線にする |

## 11. 発展ネタ（時間があれば）

- 2人対戦：色ちがいの手袋を2つ追跡して左右で得点を競う（`ColorTracker` をもう1つ作る）
- 両手プレイ：輪郭を上位2つ取り、左右の手をそれぞれカーソルにする
- 効果音を差し替える：`colorcatch/sound.py` の `_build()` に波形を足す
- スコアを Web で表示：`scores.json` を `python3 -m http.server` で公開して廊下のモニタに出す
- 難易度選択：待機画面でボタン長押し → こどもモード（時間45秒・爆弾なし）

## ファイル構成

```
main.py              起動口（引数処理）
selftest.py          画面なしの自己テスト
config.json          設定（色あわせ結果もここに保存される）
run.sh / setup.sh / install_autostart.sh
marker_card.pdf      印刷用マーカー
colorcatch/
  app.py       画面・入力・状態遷移
  engine.py    ゲームロジック（描画非依存・テスト対象）
  tracker.py   カメラ・色マーカー追跡・素手(肌色)追跡
  gpio_io.py   ボタン/LED（無い環境でも動く）
  sound.py     効果音の合成
  ranking.py   スコア保存
  text.py      文言とフォント（日本語フォントが無ければ英語表示）
  config.py    設定の読み書き
```
